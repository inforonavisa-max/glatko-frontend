// Empty stub: under vitest, `import "server-only"` resolves here (see vitest.config.ts
// resolve.alias). In the real Next build the genuine `server-only` package is used,
// which throws if a server-only module is pulled into a client bundle.
export {};
